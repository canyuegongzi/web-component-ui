{
  "name": "root",
  "private": true,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "install:all": "npm install && cd example-ui && npm install && cd packages/web-core && npm install && cd packages/web-ui && npm install"
  },
  "engines": {
    "node": ">= 10",
    "npm": ">= 5"
  },
  "files": [
    "dist",
    "LICENSE",
    "readme.md"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {}
}
